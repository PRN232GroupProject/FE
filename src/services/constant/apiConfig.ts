const devUrl = "https://localhost:7099/api/";

// Check if we're in production environment

// Set BASE_URL based on environment
export const BASE_URL = devUrl;

const apiConfig = {
    BASE_URL: devUrl,
};

export default apiConfig;
